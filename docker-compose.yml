services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: gtr
      POSTGRES_PASSWORD: gtr
      POSTGRES_DB: gtr
    ports:
      - "5432:5432"
    volumes:
      - pg:/var/lib/postgresql/data

volumes:
  pg:
